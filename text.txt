ALTER TABLE `receipts`
ADD COLUMN `customer_company_id` BIGINT UNSIGNED NULL
AFTER `company_id`,
ADD CONSTRAINT `receipts_customer_company_id_foreign`
    FOREIGN KEY (`customer_company_id`)
    REFERENCES `customer_companies` (`id`)
    ON DELETE SET NULL;

ALTER TABLE `accounts`
ADD COLUMN `branch_id` BIGINT UNSIGNED NULL
AFTER `company_id`,
ADD CONSTRAINT `accounts_branch_id_foreign`
    FOREIGN KEY (`branch_id`)
    REFERENCES `branches` (`id`)
    ON UPDATE CASCADE
    ON DELETE RESTRICT;
